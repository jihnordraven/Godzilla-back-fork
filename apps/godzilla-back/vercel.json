{
  "version": 2,
  "name": "godzilla-back",
  "builds": [{ "src": "src/main.ts", "use": "@vercel/node" }],
  "routes": [{ "src": "/(.*)", "dest": "src/main.ts" }],
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/api/v1/$1"
    }
  ]
}
